#!/usr/bin/env python3
"""this task isto get tranpose of matrix using built-in numpy library."""

import numpy as np


def np_transpose(matrix):
    """this is documentation for transposing matrix."""
    new_matrix = np.transpose(matrix)
    return new_matrix
